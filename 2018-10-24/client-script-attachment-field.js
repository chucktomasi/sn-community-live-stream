// Check if an attachment field has a value
function onChange(control, oldValue, newValue, isLoading, isTemplate) {
// 	if (isLoading || newValue === '') {
// 		return;
// 	}

	g_form.addInfoMessage(g_scratchpad.document);
// 	if (newValue != '') {
// 		g_form.addInfoMessage('has attachment')
// 	} else {
// 		g_form.addInfoMessage('no attachment');
// 	}

}