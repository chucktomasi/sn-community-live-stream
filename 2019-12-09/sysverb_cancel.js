// Use sysverb_cancel in the UI action field Action name
// to ignore mandatory fields. This script is just to test.
current.update();
gs.addInfoMessage('Saved draft');
action.setRedirectURL(current);